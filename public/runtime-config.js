// Runtime configuration — edit this file to change the API URL without rebuilding.
// For local development, leave TOKEN_SERVER_URL as empty string (uses CRA proxy).
// For AWS deployment, set to your API Gateway URL (e.g. "https://abc123.execute-api.us-east-1.amazonaws.com").
window.__RUNTIME_CONFIG__ = {
  TOKEN_SERVER_URL: "",
};
